{
  "name": "webhook-retry-engine",
  "version": "1.0.0",
  "description": "Reliable webhook delivery service with retries and idempotent ingestion",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "jest --runInBand"
  },
  "keywords": [
    "webhook",
    "retry",
    "idempotency",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "sql.js": "^1.14.2"
  },
  "devDependencies": {
    "jest": "^30.5.1",
    "supertest": "^7.2.2"
  }
}
